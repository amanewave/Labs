#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void per(int a, int b, int c,int p);

int main() {
	per (1, 4, 5);
	printf("p= %d\n",p)
	
	return 0;
}

void per(int a, int b, int c) {
	float p = a + b + c;
	print("Perimetr = %d\n",p);
}
